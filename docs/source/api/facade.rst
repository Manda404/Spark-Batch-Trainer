Public Facade
=============

.. autofunction:: spark_batch_trainer.create_trainer

The package root also exposes ``XGBoostTrainer``, ``CatBoostTrainer``, and
``LightGBMTrainer`` through lazy imports.
